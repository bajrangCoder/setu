//! Theme module for Setu
//!
//! Uses gpui-component's theme system with custom Teal accent.
//! For all colors: use `cx.theme()` via `gpui_component::ActiveTheme`

mod gpui_theme;

pub use gpui_theme::*;
